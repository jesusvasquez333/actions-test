name: ci
on: push

jobs:
  ci:
    name: CI tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Compile
        run: g++ -o test test.cpp
        shell: bash
